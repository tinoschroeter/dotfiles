- name: "Install NPM-distributed command-line tools"
  npm:
    global: yes
    name: "{{ item }}"
  with_items:
    - snyk    # Find and automatically fix vulnerabilities in your code.
    - swagger # Swagger utilities and project lifecycle support.
  become: yes
